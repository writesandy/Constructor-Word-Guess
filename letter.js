var Letter = {
    guess = guess,
    guessedAlready = boolean,
    guessChecker = function() {
        if (guessedAlready = true) {
            // if letter has been guessed correctly then populate in string
        } else {
            // if letter has not been correctly guessed then populate a underscore/blank
        }
    }
};